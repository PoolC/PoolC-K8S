# Fluent Bit

Fluent Bit is currently present in the `monitoring` namespace but is not yet represented by a standalone Helm release in PKS.

Keep the current running state in place until its installed manifests and values are recovered and converted into an ArgoCD Application.
